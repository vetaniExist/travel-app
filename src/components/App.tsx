import React, { useState } from "react";

const App: React.FC = () => {
  const [state, setState] = useState(1);
  return (
    <>
      <header></header>
      <main></main>
      <footer></footer>
    </>
  );
};

export default App;
